<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PixChest.Views.Thumbnails.ThumbnailPickerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PixChest.Views.Thumbnails"
    xmlns:thumbnailPickerV="using:PixChest.FileTypes.Views.ThumbnailPickers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:controls="using:CommunityToolkit.WinUI.Controls"
	mc:Ignorable="d"
    Title="Thumbnail Picker">
	<controls:SwitchPresenter
				Value="{x:Bind ViewModel.ThumbnailPickerViewModel.Value ,Mode=OneWay}">
		<controls:Case Value="{x:Bind ViewModel.ImageThumbnailPickerViewModel, Mode=OneTime}">
			<thumbnailPickerV:ImageThumbnailPickerView DataContext="{x:Bind ViewModel.ImageThumbnailPickerViewModel}"/>
		</controls:Case>
		<controls:Case Value="{x:Bind ViewModel.PdfThumbnailPickerViewModel, Mode=OneTime}">
			<thumbnailPickerV:PdfThumbnailPickerView DataContext="{x:Bind ViewModel.PdfThumbnailPickerViewModel}"/>
		</controls:Case>
		<controls:Case Value="{x:Bind ViewModel.VideoThumbnailPickerViewModel, Mode=OneTime}">
			<thumbnailPickerV:VideoThumbnailPickerView DataContext="{x:Bind ViewModel.VideoThumbnailPickerViewModel}"/>
		</controls:Case>
	</controls:SwitchPresenter>
</Window>

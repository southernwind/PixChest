<local:FolderGroupExecutionConfigViewUserControl
    x:Class="MediaDeck.MediaItemTypes.UI.FolderGroup.Views.FolderGroupExecutionConfigView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediaDeck.MediaItemTypes.UI.FolderGroup.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:enum="using:MediaDeck.Composition.Enum"
    xmlns:ctConverters="using:CommunityToolkit.WinUI.Converters"
    xmlns:conv="using:MediaDeck.MediaItemTypes.UI.Base.Converters"
    x:DataType="local:FolderGroupExecutionConfigView"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ctConverters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        
    </UserControl.Resources>

    <Grid Margin="0,10,0,0" RowSpacing="8" ColumnDefinitions="Auto,*">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Execution type -->
        <TextBlock Grid.Row="0" Grid.Column="0" x:Uid="ExecConfig_ExecutionTypeLabel" VerticalAlignment="Center" Margin="0,0,8,0" />
        <ComboBox Grid.Row="0" Grid.Column="1"
                  HorizontalAlignment="Stretch"
                  ItemsSource="{x:Bind ViewModel.ExecutionTypeConditions, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.ExecutionType.Value, Mode=TwoWay}"
                  x:Uid="ExecConfig_ExecutionType" />

        <!-- IsDefault -->
        <TextBlock Grid.Row="1" Grid.Column="0" x:Uid="ExecConfig_IsDefaultLabel" VerticalAlignment="Center" Margin="0,0,8,0" />
        <ToggleSwitch Grid.Row="1" Grid.Column="1"
                      IsOn="{x:Bind ViewModel.IsDefault.Value, Mode=TwoWay}"
                      x:Uid="ExecConfig_IsDefault" />

        <!-- File Path -->
        <TextBlock Grid.Row="2" Grid.Column="0" x:Uid="ExecConfig_FilePathLabel" VerticalAlignment="Center" Margin="0,0,8,0" />
        <TextBox Grid.Row="2" Grid.Column="1" MinWidth="200" Padding="0" Margin="0,0,0,8"
                 Text="{x:Bind ViewModel.Path.Value, Mode=TwoWay}"
                 x:Uid="ExecConfig_FilePath" />

        <!-- Arguments -->
        <TextBlock Grid.Row="3" Grid.Column="0" x:Uid="ExecConfig_ArgsLabel" VerticalAlignment="Center" Margin="0,0,8,0" />
        <TextBox Grid.Row="3" Grid.Column="1" MinWidth="200" Padding="0" Margin="0,0,0,8"
                 Text="{x:Bind ViewModel.Args.Value, Mode=TwoWay}"
                 x:Uid="ExecConfig_Args" />
    </Grid>
</local:FolderGroupExecutionConfigViewUserControl>

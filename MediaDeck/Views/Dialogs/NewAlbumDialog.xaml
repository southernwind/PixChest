<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
	x:Class="MediaDeck.Views.Dialogs.NewAlbumDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	mc:Ignorable="d"
	Title="アルバムを新規作成"
	PrimaryButtonText="作成"
	CloseButtonText="キャンセル"
	DefaultButton="Primary"
	PrimaryButtonClick="ContentDialog_PrimaryButtonClick" >
	<StackPanel Spacing="8">
		<TextBlock Text="アルバム名（必須）" />
		<TextBox x:Name="AlbumNameTextBox"
			Text="{x:Bind AlbumName, Mode=TwoWay}"
			PlaceholderText="北海道"
			Loaded="AlbumNameTextBox_Loaded" />
		<TextBlock Text="アルバムパス（任意。例: 旅行/2024）" />
		<TextBox Text="{x:Bind AlbumPath, Mode=TwoWay}" PlaceholderText="旅行/2024" />
	</StackPanel>
</ContentDialog>

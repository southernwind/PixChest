<?xml version="1.0" encoding="utf-8" ?>
<local:DetailSelectorUserControl x:Class="MediaDeck.Views.Panes.DetailPanes.DetailSelector"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views.Panes.DetailPanes"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d" >

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!-- Header -->
		<Grid Grid.Row="0"
			Padding="12,10"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
			BorderThickness="0,0,0,1" >
			<StackPanel Orientation="Horizontal"
				Spacing="8"
				VerticalAlignment="Center" >
				<FontIcon Glyph="&#xE946;"
					FontSize="16" />
				<TextBlock x:Uid="Details_Header"
					Style="{ThemeResource BodyStrongTextBlockStyle}"
					VerticalAlignment="Center" />
			</StackPanel>
		</Grid>

		<!-- Content -->
		<Border Grid.Row="1"
			Background="{ThemeResource LayerFillColorDefaultBrush}" >
			<ScrollViewer Padding="12"
				Visibility="{x:Bind ViewModel.TargetFiles.Value, Mode=OneWay, Converter={StaticResource NullToCollapseConverter}}" >
				<StackPanel Spacing="16" >
					<local:PropertiesDetail ViewModel="{x:Bind ViewModel,Mode=OneWay}" />
					<local:TagsDetail ViewModel="{x:Bind ViewModel,Mode=OneWay}" />
				</StackPanel>
			</ScrollViewer>
		</Border>
	</Grid>
</local:DetailSelectorUserControl>
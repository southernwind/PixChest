<?xml version="1.0" encoding="utf-8" ?>
<local:AlbumRepositoryUserControl x:Class="MediaDeck.Views.Panes.RepositoryPanes.AlbumRepository"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views.Panes.RepositoryPanes"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:objects="using:MediaDeck.Core.Models.Repositories.Objects"
	mc:Ignorable="d" >

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<CheckBox Grid.Row="0"
			IsChecked="{x:Bind ViewModel.AlbumRepositoryViewModel.IncludeSubAlbums.Value,Mode=TwoWay}" >
			<TextBlock Text="Include Sub Albums" />
		</CheckBox>
		<TreeView Grid.Row="1"
			ItemsSource="{x:Bind ViewModel.AlbumRepositoryViewModel.RootAlbum.Value.ChildAlbums, Mode=OneWay}"
			SelectedItem="{x:Bind ViewModel.AlbumRepositoryViewModel.SelectedAlbum.Value,Mode=TwoWay}" >
			<TreeView.ItemTemplate>
				<DataTemplate x:DataType="objects:AlbumObject" >
					<TreeViewItem AutomationProperties.Name="{x:Bind AlbumName}"
						ItemsSource="{x:Bind ChildAlbums}"
						DoubleTapped="TreeViewItem_DoubleTapped"
						IsExpanded="{x:Bind IsExpanded}" >
						<StackPanel Orientation="Horizontal" >
							<FontIcon Glyph="&#xE8FD;" />
							<TextBlock Margin="0,0,10,0" />
							<TextBlock>
								<Run Text="{x:Bind AlbumName}" />
								<Run Text=" (" />
								<Run Text="{x:Bind FileCount}" />
								<Run Text=")" />
							</TextBlock>
						</StackPanel>
					</TreeViewItem>
				</DataTemplate>
			</TreeView.ItemTemplate>
		</TreeView>
	</Grid>
</local:AlbumRepositoryUserControl>

<?xml version="1.0" encoding="utf-8" ?>
<local:BackgroundTasksStatusIndicatorUserControl
	x:Class="MediaDeck.Views.Panes.StatusPanes.BackgroundTasksStatusIndicator"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views.Panes.StatusPanes"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:toolsViewModels="using:MediaDeck.ViewModels.Tools"
	mc:Ignorable="d">

	<Button
		Padding="10,0"
		VerticalAlignment="Center"
		HorizontalAlignment="Left"
		Background="Transparent"
		BorderThickness="0">
		<StackPanel Orientation="Horizontal"
			Spacing="8"
			VerticalAlignment="Center">
			<ProgressRing Width="16"
				Height="16"
				IsActive="{x:Bind ViewModel.HasRunningTasks.Value, Mode=OneWay}"
				Visibility="{x:Bind ToVisibility(ViewModel.HasRunningTasks.Value), Mode=OneWay}" />
			<FontIcon Glyph="&#xE823;"
				FontSize="14"
				Foreground="{ThemeResource TextFillColorSecondaryBrush}"
				Visibility="{x:Bind ToInverseVisibility(ViewModel.HasRunningTasks.Value), Mode=OneWay}" />
			<TextBlock Text="{x:Bind ViewModel.SummaryText.Value, Mode=OneWay}"
				Style="{StaticResource CaptionTextBlockStyle}"
				VerticalAlignment="Center" />
		</StackPanel>
		<Button.Flyout>
			<Flyout Placement="TopEdgeAlignedLeft">
				<Grid Width="380"
					MaxHeight="420"
					RowSpacing="12">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<TextBlock x:Uid="StatusBar_BackgroundTasks"
						Style="{StaticResource BodyStrongTextBlockStyle}" />
					<ListView Grid.Row="1"
						ItemsSource="{x:Bind ViewModel.TaskItems, Mode=OneWay}"
						SelectionMode="None"
						IsItemClickEnabled="False"
						MaxHeight="340">
						<ListView.ItemTemplate>
							<DataTemplate x:DataType="toolsViewModels:BackgroundTaskStatusItemViewModel">
								<Grid Padding="0,8"
									ColumnSpacing="12"
									RowSpacing="6">
									<Grid.RowDefinitions>
										<RowDefinition Height="Auto" />
										<RowDefinition Height="Auto" />
									</Grid.RowDefinitions>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="*" />
										<ColumnDefinition Width="Auto" />
									</Grid.ColumnDefinitions>
									<StackPanel Orientation="Horizontal"
										Spacing="8">
										<ProgressRing Width="14"
											Height="14"
											IsActive="{x:Bind IsRunning.Value, Mode=OneWay}"
											Visibility="{x:Bind IsRunning.Value, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
										<TextBlock Text="{x:Bind DisplayName}"
											Style="{StaticResource BodyTextBlockStyle}"
											TextTrimming="CharacterEllipsis" />
										<TextBlock Text="{x:Bind StatusText.Value, Mode=OneWay}"
											Style="{StaticResource CaptionTextBlockStyle}"
											Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
									</StackPanel>
									<StackPanel Grid.Column="1"
										Orientation="Horizontal"
										Spacing="4">
										<Button MinWidth="0"
											Padding="8,4"
											Command="{x:Bind ReRunCommand, Mode=OneWay}"
											Visibility="{x:Bind IsRunning.Value, Mode=OneWay, Converter={StaticResource InvertBoolToVisibilityConverter}}"
											x:Uid="Common_ReRun">
											<FontIcon Glyph="&#xE72C;"
												FontSize="12" />
										</Button>
										<Button MinWidth="0"
											Padding="8,4"
											Command="{x:Bind CancelCommand, Mode=OneWay}"
											Visibility="{x:Bind IsRunning.Value, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
											x:Uid="Common_CancelButton">
											<FontIcon Glyph="&#xE711;"
												FontSize="12" />
										</Button>
									</StackPanel>
									<Grid Grid.Row="1"
										Grid.ColumnSpan="2"
										ColumnSpacing="8">
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="*" />
											<ColumnDefinition Width="Auto" />
										</Grid.ColumnDefinitions>
										<ProgressBar Minimum="0"
											Maximum="{x:Bind TargetValue.Value, Mode=OneWay}"
											Value="{x:Bind CompletedValue.Value, Mode=OneWay}" />
										<TextBlock Grid.Column="1"
											Text="{x:Bind ProgressText.Value, Mode=OneWay}"
											Style="{StaticResource CaptionTextBlockStyle}"
											Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
									</Grid>
								</Grid>
							</DataTemplate>
						</ListView.ItemTemplate>
					</ListView>
				</Grid>
			</Flyout>
		</Button.Flyout>
	</Button>
</local:BackgroundTasksStatusIndicatorUserControl>

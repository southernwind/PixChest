<?xml version="1.0" encoding="utf-8" ?>
<local:TagManagerViewUserControl x:Class="MediaDeck.Views.Tags.TagManagerView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views.Tags"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:tags="using:MediaDeck.ViewModels.Tags"
	xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
	mc:Ignorable="d" >

	<Grid x:Name="Root"
		Padding="12" >
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="220" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="220" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>

		<!-- タグカテゴリ一覧 -->
		<Grid Grid.Column="0" >
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="8" />
				<RowDefinition Height="*" />
				<RowDefinition Height="8" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="8" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<TextBlock Grid.Row="0"
				x:Uid="TagManager_Category"
				FontWeight="SemiBold"
				FontSize="14" />

			<Border Grid.Row="2"
				BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
				BorderThickness="1"
				CornerRadius="4" >
				<ListBox ItemsSource="{x:Bind ViewModel.TagCategories, Mode=OneWay}"
					SelectedItem="{x:Bind ViewModel.SelectedTagCategory.Value, Mode=TwoWay}" >
					<ItemsControl.ItemTemplate>
						<DataTemplate x:DataType="tags:TagCategoryViewModel" >
							<TextBlock Text="{x:Bind TagCategoryName.Value}"
								TextTrimming="CharacterEllipsis" />
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ListBox>
			</Border>

			<StackPanel Grid.Row="4"
				Orientation="Horizontal"
				Spacing="8" >
				<Button Command="{x:Bind ViewModel.AddTagCategoryCommand}"
					ToolTipService.ToolTip="新しいタグカテゴリを追加" >
					<StackPanel Orientation="Horizontal"
						Spacing="6" >
						<FontIcon Glyph="&#xF8AA;"
							FontSize="12" />
						<TextBlock x:Uid="Common_Add" />
					</StackPanel>
				</Button>
			</StackPanel>

			<!-- カテゴリ編集エリア -->
			<StackPanel Grid.Row="6"
				Spacing="6"
				Visibility="{x:Bind ViewModel.SelectedTagCategory.Value, Mode=OneWay, Converter={StaticResource NullToCollapseConverter}}" >
				<TextBlock x:Uid="TagManager_CategoryName"
					FontWeight="SemiBold" />
				<TextBox
					Text="{x:Bind ViewModel.SelectedTagCategory.Value.TagCategoryName.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
					x:Uid="TagManager_CategoryNameInput" />
				<TextBlock x:Uid="TagManager_Detail"
					FontWeight="SemiBold" />
				<TextBox
					Text="{x:Bind ViewModel.SelectedTagCategory.Value.Detail.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
					x:Uid="TagManager_DetailInput" />
				<Button Click="DeleteTagCategoryButton_Click"
					Margin="0,8,0,0"
					HorizontalAlignment="Right"
					IsEnabled="{x:Bind ViewModel.SelectedTagCategory.Value.IsDeletable, Mode=OneWay}" >
					<StackPanel Orientation="Horizontal" Spacing="6">
						<FontIcon Glyph="&#xE74D;" FontSize="12" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
						<TextBlock x:Uid="TagManager_DeleteCategory" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
					</StackPanel>
				</Button>
			</StackPanel>
		</Grid>


		<!-- 区切り2 -->
		<toolkit:GridSplitter Grid.Column="1"
			Width="4"
			HorizontalAlignment="Center"
			ResizeBehavior="BasedOnAlignment" />
		
		<!-- タグ一覧 -->
		<Grid Grid.Column="2" >
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="8" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="8" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<TextBlock Grid.Row="0"
				x:Uid="TagManager_Tags"
				FontWeight="SemiBold"
				FontSize="14" />

			<TextBox Grid.Row="2"
				Text="{x:Bind ViewModel.SelectedTagCategory.Value.FilterText.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
				x:Uid="TagManager_SearchTags" />

			<Border Grid.Row="4"
				BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
				BorderThickness="1"
				CornerRadius="4" >
				<ListBox ItemsSource="{x:Bind ViewModel.SelectedTagCategory.Value.FilteredTags, Mode=OneWay}"
					SelectedItem="{x:Bind ViewModel.SelectedTagCategory.Value.SelectedTag.Value, Mode=TwoWay}" >
					<ItemsControl.ItemTemplate>
						<DataTemplate x:DataType="tags:TagViewModel" >
							<StackPanel Spacing="2" >
								<TextBlock TextTrimming="CharacterEllipsis" >
									<Run Text="{x:Bind TagName.Value, Mode=OneWay}" />
									<Run Text=" (" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
									<Run Text="{x:Bind UsageCount.Value, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
									<Run Text=")" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
								</TextBlock>
								<TextBlock
									Visibility="{x:Bind RepresentativeText.Value, Mode=OneWay, Converter={StaticResource NullToCollapseConverter}}"
									Text="{x:Bind RepresentativeText.Value, Mode=OneWay}"
									FontSize="10"
									Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
							</StackPanel>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ListBox>
			</Border>
		</Grid>

		<!-- 区切り2 -->
		<toolkit:GridSplitter Grid.Column="3"
			Width="4"
			HorizontalAlignment="Center"
			ResizeBehavior="BasedOnAlignment" />

		<!-- タグ編集エリア -->
		<Border Grid.Column="4"
			Visibility="{x:Bind ViewModel.SelectedTagCategory.Value.SelectedTag.Value, Mode=OneWay, Converter={StaticResource NullToCollapseConverter}}"
			Padding="16"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
			BorderThickness="1"
			CornerRadius="8" >
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="16" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="16" />
					<RowDefinition Height="*" />
					<RowDefinition Height="8" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<!-- タグ名入力 -->
				<StackPanel Grid.Row="0"
					Spacing="6" >
					<TextBlock x:Uid="TagManager_TagName"
						FontWeight="SemiBold" />
					<TextBox
						Text="{x:Bind ViewModel.SelectedTagCategory.Value.SelectedTag.Value.TagName.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
						x:Uid="TagManager_TagNameInput" />
					
					<TextBlock x:Uid="TagManager_Ruby"
						FontWeight="SemiBold"
						Margin="0,8,0,0" />
					<TextBox
						Text="{x:Bind ViewModel.SelectedTagCategory.Value.SelectedTag.Value.Ruby.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
						x:Uid="TagManager_RubyInput" />
				</StackPanel>

				<!-- カテゴリ選択 -->
				<StackPanel Grid.Row="2"
					Spacing="6" >
					<TextBlock x:Uid="TagManager_ParentCategory"
						FontWeight="SemiBold" />
					<ComboBox HorizontalAlignment="Stretch"
						ItemsSource="{x:Bind ViewModel.TagCategories, Mode=OneWay}"
						SelectedItem="{x:Bind ViewModel.SelectedTagCategory.Value.SelectedTag.Value.TagCategory.Value, Mode=TwoWay}" >
						<ComboBox.ItemTemplate>
							<DataTemplate x:DataType="tags:TagCategoryViewModel" >
								<TextBlock Text="{x:Bind TagCategoryName.Value}" />
							</DataTemplate>
						</ComboBox.ItemTemplate>
					</ComboBox>
				</StackPanel>

				<!-- エイリアス一覧 -->
				<Grid Grid.Row="4" >
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="8" />
						<RowDefinition Height="*" />
					</Grid.RowDefinitions>

					<TextBlock Grid.Row="0"
						x:Uid="TagManager_Aliases"
						FontWeight="SemiBold" />

					<Border Grid.Row="2"
						BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
						BorderThickness="1"
						CornerRadius="4" >
						<ScrollViewer>
							<ItemsControl
								ItemsSource="{x:Bind ViewModel.SelectedTagCategory.Value.SelectedTag.Value.TagAliases, Mode=OneWay}" >
								<ItemsControl.ItemTemplate>
									<DataTemplate x:DataType="tags:TagAliasViewModel" >
										<Grid Padding="8"
											BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
											BorderThickness="0,0,0,1" >
											<Grid.ColumnDefinitions>
												<ColumnDefinition Width="*" />
												<ColumnDefinition Width="8" />
												<ColumnDefinition Width="*" />
												<ColumnDefinition Width="8" />
												<ColumnDefinition Width="Auto" />
											</Grid.ColumnDefinitions>
											<TextBox Grid.Column="0"
												Text="{x:Bind Alias.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
												x:Uid="TagManager_AliasInput" />
											<TextBox Grid.Column="2"
												Text="{x:Bind Ruby.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
												x:Uid="TagManager_RubyInputShort" />
											<Button Grid.Column="4"
												Command="{Binding DataContext.SelectedTagCategory.Value.SelectedTag.Value.RemoveTagAliasCommand, ElementName=Root}"
												CommandParameter="{x:Bind Mode=OneWay}"
												x:Uid="Common_Delete_ToolTip"
												Padding="6" >
												<FontIcon Glyph="&#xF78A;"
													FontSize="12" />
											</Button>
										</Grid>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</ScrollViewer>
					</Border>
				</Grid>

				<!-- ボタンエリア -->
				<Grid Grid.Row="6">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto" />
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>
					
					<!-- エイリアス追加ボタン -->
					<Button Grid.Column="0"
						Command="{x:Bind ViewModel.SelectedTagCategory.Value.SelectedTag.Value.AddTagAliasCommand, Mode=OneWay}" >
						<StackPanel Orientation="Horizontal"
							Spacing="6" >
							<FontIcon Glyph="&#xF8AA;"
								FontSize="12" />
							<TextBlock x:Uid="TagManager_AddAlias" />
						</StackPanel>
					</Button>

					<!-- タグ削除ボタン -->
					<Button Grid.Column="2" 
						Click="DeleteTagButton_Click" >
						<StackPanel Orientation="Horizontal" Spacing="6">
							<FontIcon Glyph="&#xE74D;" FontSize="12" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
							<TextBlock x:Uid="TagManager_DeleteTag" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
						</StackPanel>
					</Button>
				</Grid>
			</Grid>
		</Border>
	</Grid>
</local:TagManagerViewUserControl>
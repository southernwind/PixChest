<?xml version="1.0" encoding="utf-8" ?>
<Window
	x:Class="MediaDeck.Views.SplashScreenWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:MediaDeck.Views"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:viewmodels="using:MediaDeck.ViewModels"
	mc:Ignorable="d">

	<Window.SystemBackdrop>
		<DesktopAcrylicBackdrop/>
	</Window.SystemBackdrop>

	<Grid Background="Transparent">
		<Border x:Name="RootBorder"
				Background="Transparent"
				BorderThickness="1"
				BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}">
			<Grid>
				<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="32">
					<!-- アプリロゴ/スプラッシュ画像 -->
					<Image Source="ms-appx:///Assets/SplashScreen.scale-200.png" 
						Width="420" 
						Height="Auto"
						Stretch="Uniform"
						HorizontalAlignment="Center"
						VerticalAlignment="Center"/>

					<StackPanel Spacing="12">
						<ProgressBar IsIndeterminate="True" 
							Width="240" 
							HorizontalAlignment="Center" 
							VerticalAlignment="Center"/>

						<TextBlock x:Name="StatusTextBlock"
							Text="{x:Bind ViewModel.StatusMessage.Value, Mode=OneWay}"
							HorizontalAlignment="Center" 
							FontSize="14"
							Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
					</StackPanel>
				</StackPanel>

				<!-- 下部にバージョン情報を表示 -->
				<StackPanel HorizontalAlignment="Right" 
					VerticalAlignment="Bottom" 
					Margin="16"
					Orientation="Horizontal"
					Spacing="4">
					<TextBlock Text="MediaDeck" 
						FontSize="12"
						FontWeight="SemiBold"
						Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
					<TextBlock Text="{x:Bind ViewModel.AppVersion}" 
						FontSize="12"
						Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
				</StackPanel>
			</Grid>
		</Border>
	</Grid>
</Window>
